## PROGRESS TRACKING WITH HOOKS

### CRITICAL RESTRICTIONS
- **NEVER** update status in task documentation
- **NEVER** create progress reports because hooks generate them automatically
- **NEVER** maintain checklists outside the automated system
- **NEVER** track timestamps because hooks capture all timing
- **NEVER** document state changes because hooks persist all transitions
- **NEVER** delay implementation work to update progress
